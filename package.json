{
  "name": "morelia",
  "module": "src/index.ts",
  "version": "1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "bundle": "bun build src/index.ts --outdir bundle --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
