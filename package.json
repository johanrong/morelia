{
  "name": "morelia",
  "module": "index.ts",
  "version": "1.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "bundle": "bun build index.ts --outdir bundle --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
